#include <stdio.h>
int main(){
    int A,B,T,E;
    int i=1;
    A=B=E=0;
    scanf("%d", &T);
    while(i<=T){
        scanf("%d", &E);
        if (E>A || !A){
            A=E;
        }
        if (E<B || !B){
            B=E;
        }
        
        i+=1;
    }
    printf("%d %d",B,A);

}
